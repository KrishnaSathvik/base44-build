import { isRouteErrorResponse, useRouteError } from 'react-router-dom';
import { Button } from '@/components/ui';
import { PageMetadata } from '@/app/PageMetadata';
export function RouteError() { const error = useRouteError(); if (import.meta.env.DEV) console.error('Route failed', error); const missing = isRouteErrorResponse(error) && error.status === 404; return <><PageMetadata title={missing?'Not Found':'Route Unavailable'} description="This VensaOS route is unavailable."/><main className="grid min-h-screen place-items-center bg-canvas px-4"><div className="max-w-md text-center"><p className="fi-eyebrow">{missing ? 'Not found' : 'Route unavailable'}</p><h1 className="fi-display mt-4 text-3xl font-medium">{missing ? 'This page does not exist.' : 'This view could not be loaded.'}</h1><p className="mt-3 text-sm text-ink-muted">If the app was just updated, reload once to recover the newest route files.</p><Button className="mt-6" onClick={() => window.location.reload()}>Reload</Button></div></main></>; }
