import { render,screen } from '@testing-library/react';
import { expect,test,vi } from 'vitest';
import { AppErrorBoundary } from '@/app/AppErrorBoundary';
function Broken():never{throw new Error('private internal detail');}
test('error boundary exposes a branded noindex recovery screen without internal details',()=>{document.head.innerHTML='<meta name="description"><meta name="robots" content="index, follow"><meta property="og:url" content="https://vensaos.com/"><link rel="canonical" href="https://vensaos.com/">';vi.spyOn(console,'error').mockImplementation(()=>undefined);render(<AppErrorBoundary><Broken/></AppErrorBoundary>);expect(screen.getByRole('heading',{name:'VensaOS could not open this view.'})).toBeInTheDocument();expect(screen.queryByText(/private internal detail/i)).not.toBeInTheDocument();expect(screen.getByRole('button',{name:/reload safely/i})).toBeInTheDocument();expect(document.querySelector('meta[name="robots"]')).toHaveAttribute('content','noindex, nofollow');expect(document.querySelector('link[rel="canonical"]')).not.toBeInTheDocument();});
